@model ProductoModel
@{
    ViewData["Title"] = "Home Page";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="card">
    <div class="card-header">Borrar producto</div>
    <div class="card-body">
        <form asp-action="Borrar" asp-controller="Producto" method="post">
            <input asp-for="Id" type="hidden" class="form-control">
            <div class="alert alert-danger" role="alert">
                Desea borrar el producto @Html.DisplayTextFor(m => m.Nombre) ?
            </div>
            <button type="submit" class="btn btn-warning">Borrar</button>
            <a asp-action="Listar" asp-controller="Producto" class="btn btn-info">Volver</a>
        </form>
    </div>
</div>